# EdgeTX compiled Lua bytecode (generated on the radio)
*.luac

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# Local virtualenv for building the documentation figures. Created on demand,
# never needed to build, deploy or run the widget.
.venv-docs/
